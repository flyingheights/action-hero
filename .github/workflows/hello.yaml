name: Hello world

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
      - uses:  phwes/simple_hello_world@v1

      - name: Say Hello
        run: echo "Hello world!"
              workflow-dispatch:

        
